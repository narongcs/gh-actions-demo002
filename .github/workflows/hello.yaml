name: Hello World
on: 
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line
        run: echo "Hello Github Actions!"
